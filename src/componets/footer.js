// Define la función 'footer' que devuelve un componente de pie de página como una cadena de texto HTML.
export const footer = () => {
  // Crea una cadena de texto HTML que representa el contenido del pie de página.
  const footerComponent = `
     <footer>
      Videojuegos de los 80's al 2023. Desarrollado por Barbara Trejo e Itzel García.
     </footer>`;

  // Retorna la cadena de texto HTML que representa el componente de pie de página.
  return footerComponent;
};
